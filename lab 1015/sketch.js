//  Your Name
// 	Date or version number
//  This is a comment
//  The setup function function is called once when your program begins
var var list = [4,6,1,8,2,9]
function setup() {
  var cnv = createCanvas(800, 800);
  cnv.position((windowWidth-width)/2, 30);
  background(255, 255, 255);
  fill(200, 30, 150);
function bubbleSort(){
    for (i=0; i<n-1; i++) {
      for (j=0; j<n-1-i; j++){
        if (list[j+1]< a[j]) {
          swap(list, i, j);
        }
      }
    }
}
function swap(a, i, j){
  var temp=a[i];
  list[i]=list[j]
  list[j]=temp;
}
for(var i=0; i<list.length; i++){
  for (var x=0)
}
